import { createQueryKeyStructure } from "@/lib/query_keys";

export const sellerApplicationQueryKey =
  createQueryKeyStructure("sellerApplication");

export const botStatusQueryKey = createQueryKeyStructure("botStatus");
export const adminSellerApplicationQueryKey = createQueryKeyStructure(
  "adminSellerApplication"
);
